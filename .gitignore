# Personal session log and agent notes (not part of the repo)
memory.md
CLAUDE.md

# Secrets (API keys)
.env

# Python / uv
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/

# Tool caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Local data (raw cache + staging); never committed
/data/

# Generated static page (deployed by the daily workflow, not committed)
/site/

# Astro front-end: dependencies, build cache and the data file `eurohoops publish` writes
web/node_modules/
web/.astro/
web/src/data/site.json

# Impeccable scratch (screenshots, decision-page state); DESIGN.md, design.json and briefs are kept
.impeccable/review/
.impeccable/questions/
.impeccable/mocks/
mlruns/
